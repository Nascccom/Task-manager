import { CSSProperties } from "react"

export const style = {
    scrollbar: {
        width: "100%",
        height: "90vh",
        overflowY: "hidden",
    } as CSSProperties,
    fieldContainer: {
        marginTop: "25px",
        minHeight: "55px",
    } as CSSProperties,
    todolistsContainer: {
        flexWrap: "nowrap",
        marginTop: "15px",
    } as CSSProperties,
}
